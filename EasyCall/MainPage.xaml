<phone:PhoneApplicationPage    
    x:Class="EasyCall.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="768"
    shell:SystemTray.IsVisible="True"
    DataContext="{Binding Main, Source={StaticResource Locator}}"
    Loaded="PhoneApplicationPage_Loaded">

    <Grid x:Name="LayoutRoot" >
        <Grid.Resources>
            <DataTemplate x:Key="NumberDataTemplate" >
                <Button Click="Call_Click" >
                    <Button.Template>
                        <ControlTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Image Source="Images\CircleCall.png" Margin="8"/>
                                <TextBlock Text="{Binding}" VerticalAlignment="Center" />
                            </StackPanel>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </DataTemplate>

            <ControlTemplate x:Key="ContactItemTemplate" TargetType="ListBoxItem">
                <StackPanel Margin="8">
                    <TextBlock Text="{Binding DisplayName}"
                               FontSize="{StaticResource PhoneFontSizeExtraLarge}" />
                    <ListBox ItemsSource="{Binding Numbers}" toolkit:TiltEffect.IsTiltEnabled="True"
                             ScrollViewer.VerticalScrollBarVisibility="Disabled"
                             ItemTemplate="{StaticResource NumberDataTemplate}" />
                </StackPanel>
            </ControlTemplate>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <toolkit:PhoneTextBox Name="SearchTextBox" InputScope="TelephoneNumber" KeyUp="SearchTextBox_KeyUp"
                              ActionIcon="Images\call.png" ActionIconTapped="SearchTextBox_ActionIconTapped"
                              Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=Explicit}" GotFocus="SearchTextBox_GotFocus" />

        <ListBox ItemsSource="{Binding SearchedContacts}" Grid.Row="1">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Template" Value="{StaticResource ContactItemTemplate}" />
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>
</phone:PhoneApplicationPage>