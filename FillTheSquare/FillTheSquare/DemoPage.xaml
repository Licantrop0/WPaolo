<phone:PhoneApplicationPage 
    x:Class="FillTheSquare.DemoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait" Orientation="Portrait"
    mc:Ignorable="d" d:DesignHeight="800" d:DesignWidth="480"
    shell:SystemTray.IsVisible="False"
    BackKeyPress="PhoneApplicationPage_BackKeyPress">

    <Grid x:Name="LayoutRoot" Background="{StaticResource BackgroundGradientBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="12,17,0,28">
            <TextBlock FontSize="36" Style="{StaticResource SquareTitleStyle}"
                       Text="{Binding Path=LocalizedResources.ThisIsADemo, Source={StaticResource LocalizedStrings}}" />
        </StackPanel>

        <StackPanel Grid.Row="1" Margin="12,0,12,0">
            <Image Source="Phil\triste.png" />
            <TextBlock FontSize="36" Foreground="{StaticResource ForegroundGradientBrush}"
                       Text="{Binding Path=LocalizedResources.DontMakePhilSad, Source={StaticResource LocalizedStrings}}" />

            <HyperlinkButton Name="BuyButton" Margin="0,0,0,8"  Foreground="{StaticResource ForegroundGradientBrush}"
                             Content="{Binding Path=LocalizedResources.BuyNow, Source={StaticResource LocalizedStrings}}"
                             FontSize="{StaticResource PhoneFontSizeExtraLarge}" Click="BuyButton_Click" />

            <TextBlock FontSize="36"  Foreground="{StaticResource ForegroundGradientBrush}"
                       Text="{Binding Path=LocalizedResources.PhilWillBeHappy, Source={StaticResource LocalizedStrings}}" />
        </StackPanel>
    </Grid>

</phone:PhoneApplicationPage>
