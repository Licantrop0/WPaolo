<Application
    x:Class="EasyCall.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:toolkit="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls.Toolkit" 
    xmlns:conv="clr-namespace:EasyCall.Converters"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d" >

    <Application.Resources>
         <conv:TextHighlighterConverter x:Key="TextHighlighter"  />
       
        <DataTemplate x:Key="groupItemHeader">
            <StackPanel Orientation="Horizontal" Margin="0,24,0,0" >
                <Image Source="{Binding Bitmap}" Width="48" Height="48"
                       Stretch="Fill" Margin="8,0" />
                <TextBlock Text="{Binding Model.DisplayName}"
                           VerticalAlignment="Center" Width="300"
                           FontSize="{StaticResource PhoneFontSizeExtraLarge}" />
            </StackPanel>
        </DataTemplate>

        <ControlTemplate x:Key="ContactNumberTemplate">
            <StackPanel Orientation="Horizontal" >
                <Rectangle Height="48" Width="48" Margin="8"
                           Fill="{StaticResource PhoneForegroundBrush}">
                    <Rectangle.OpacityMask>
                        <ImageBrush ImageSource="Images\CircleCall.png"/>
                    </Rectangle.OpacityMask>
                </Rectangle>
                <TextBlock Text="{Binding}"
                           VerticalAlignment="Center"
                           FontSize="{StaticResource PhoneFontSizeMedium}" />
            </StackPanel>
        </ControlTemplate>

    </Application.Resources>

    <Application.ApplicationLifetimeObjects>
        <!--Required object that handles lifetime events for the application-->
        <shell:PhoneApplicationService
            Launching="Application_Launching" Closing="Application_Closing"
            Activated="Application_Activated" Deactivated="Application_Deactivated" />
    </Application.ApplicationLifetimeObjects>

</Application>
